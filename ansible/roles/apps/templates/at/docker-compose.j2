version: '2'
services:
    web:
        build: "{{ apps_src_path }}at"
        restart: always
        ports:
            - "127.0.0.1:6100:8000"
        volumes:
            # sync timezone with host
            - /etc/localtime:/etc/localtime:ro
        environment:
            DEBUG: "false"

